# Evidence manifest

The source DOCX supplied during the practical session contains the working screenshots.

Recommended public evidence set:
1. DB2I Primary Option Menu — SSID D9G
2. DB2 Commands panel
3. `-DISPLAY THREAD(*)` — NORMAL COMPLETION
4. SPUFI input/output configuration
5. `SYSIBM.SYSDATABASE` successful result
6. `SQLCODE -206 / SQLSTATE 42703` troubleshooting

## Excluded until sanitized
The `-DISPLAY DDF` screenshot contains network information and must not be published in raw form.
