# Evidencias del Lab 05

Las capturas originales aportadas durante la ejecución del laboratorio se incluyen físicamente en `evidence/screenshots/`.

El conjunto muestra:
- ALTER TABLE y SQLCODE 0
- SELECT de verificación
- UPDATE de las filas
- DELETE con `NUMBER OF ROWS AFFECTED IS 1`
- SELECT posterior con 2 filas
- DROP TABLE con SQLCODE 0
- SELECT negativo posterior con SQLCODE -204 / SQLSTATE 42704
